import { render, screen } from "@testing-library/react";

import { Header } from "./Header";

describe("Header", () => {
  const siteInfo = {
    headerMenuLinks: [],

    name: "Site",

    socialMediaLinks: [],

    url: "https://example.com",
  };

});
